server:
  port: 8080
spring:
  application:
    name: "accounts"
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  config:
    import: "configserver:http://localhost:8071/"
#  config:
#    import:
#      - "application_prod.yml"
#    activate:
#      on-profile: "prod"
#configserver->use this name exact in the url for all it is fixed and using the using config server we can define properties now to connect
#optional is if soone doesnot need configuartion to start the microservice we define it as like that -optional:configserver:http//localhost:8071/